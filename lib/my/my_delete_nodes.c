/*
** ETNA PROJECT, 08/10/2020 by elisab_a
** my_delete_nodes
** File description:
**      [...]
*/

#include <stddef.h>
#include <stdlib.h>
#include "../include/my.h"

// void my_delete_nodes(linked_list_t **list, const int data_ref)
// {
//     linked_list_t *prev = *list;
//     linked_list_t *current = *list;

//     while(current != NULL){
//         if(current->data == data_ref && prev == current){
//             prev->next = current->next;
//         } else if (current->data == data_ref && prev == current) {
//             current = current->next;
//             prev = prev ->next;
//         }
//     }

//     *list = prev;
    
// }